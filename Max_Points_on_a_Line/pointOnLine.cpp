#include <iostream>
#include <vector>

using namespace std;


class Solution {
public:
    int maxPoints(vector<vector<int> >& points) {
        
    }

	bool constructLine(vector<vector<int> > v) {
		
	}
};
